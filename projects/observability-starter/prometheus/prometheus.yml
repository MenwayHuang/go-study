global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "observability_starter"
    metrics_path: /metrics
    static_configs:
      # 如果你用 docker compose 启动 Prometheus，而服务跑在宿主机：
      # Windows / Mac 一般用 host.docker.internal
      - targets: ["host.docker.internal:8080"]
